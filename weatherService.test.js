    import {
    calculateScore
} from "./weatherService.js";





/*describe("calculateScore", () => {
    test("returns 100 for safe weather", () => {
        const score = calculateScore({ precipitation: 0, windSpeed: 5, uvIndex: 3, visibility: 10 });
        expect(score).toBe(100);
    });
});

describe("calculateScore reduces points for heavy rain, wind, high UV, and low visibility", () => {
    test("returns 20 for dangerous weather", () => {
        const weather = { precipitation: 70, windSpeed: 40, uvIndex: 10, visibility: 2 };
        const score = calculateScore(weather);
        expect(score).toBe(20);
    });
});*/


